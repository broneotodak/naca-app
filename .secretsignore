# Allowlist for the Secret Scan workflow (.github/workflows/secret-scan.yml).
# One entry per line: a filename, glob, or regex (matched against the path),
# or a token (matched against a line's content). Comment lines start with #.

# safe telemetry metric keys
tokensIn
tokensOut
llm_tokens_in
llm_tokens_out

# The scanner workflow itself holds the regex DEFINITIONS (ccc_sk_, eyJ…,
# postgres://…). They describe what to detect; they aren't secrets.
.github/workflows/secret-scan.yml

# lib/config.dart.example carries the PUBLIC Supabase anon key (eyJ…). It is
# RLS-gated and designed to ship in the client — public by intent, not a leak.
# (The real backend authToken is injected at deploy from the NACA_AUTH_TOKEN
# secret; config.dart itself is gitignored — see deploy.yml + the template.)
lib/config.dart.example
